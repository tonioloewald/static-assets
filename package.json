{
  "name": "@tosijs/static-assets",
  "private": true,
  "type": "module",
  "description": "Mirror source for cdn.tosijs.net — a shared static-asset CDN for tosijs.net projects.",
  "scripts": {
    "scan": "bun bin/scan-conversions.ts",
    "convert": "bun bin/convert.ts",
    "build": "bun bin/convert.ts && bun bin/mirror.ts",
    "publish": "bun bin/publish.ts",
    "deploy": "bun run build && bunx wrangler pages deploy public --project-name cdn-tosijs",
    "deploy:firebase": "bun run build && bunx firebase-tools deploy --only hosting"
  }
}
